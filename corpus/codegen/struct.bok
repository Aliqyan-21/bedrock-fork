// output: 60
type Point = struct
    x: i32,
    y: i32,
end

func main() -> i32
    const a: i32 = 10;
    var p1 = Point where x = a, y = a end
    var p2 = Point where x = 10, y = 10 end
    var p3: Point = _ where x = 10, y = 10 end
    return p1.x + p1.y + p2.x + p2.y + p3.x + p3.y;
end
